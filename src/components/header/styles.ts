import styled from 'styled-components'

export const HeaderContainer = styled.header`
  display: flex;
  justify-content: center;
  padding: 4rem 0 8.375rem;

  background: url("cover.svg") center center;
  background-size: cover;
`
